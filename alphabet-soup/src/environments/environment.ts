// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false
};

export const firebaseConfig = {
  apiKey: 'AIzaSyC8e9_AxNaMUGdwH-wdwkkyjvqCkgrY0wQ',
  authDomain: 'alphabet-soup-9bd5c.firebaseapp.com',
  databaseURL: 'https://alphabet-soup-9bd5c.firebaseio.com',
  projectId: 'alphabet-soup-9bd5c',
  storageBucket: 'alphabet-soup-9bd5c.appspot.com',
  messagingSenderId: '79943474220'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
